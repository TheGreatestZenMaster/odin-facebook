<%= form_for(current_user.likes.find_by(liked_post_id: @post.id), 
                                html: {method: :delete}, remote: true) do |f| %>
    <%= f.submit "Unlike", class: "fa fa-reply" %>
<% end %>

<%#, remote: true %>